import { Directive } from '@angular/core';

@Directive({
  selector: '[cmeViewContainerPaletteHeader]'
})
export class ViewContainerPaletteHeaderDirective {
  // marker directive to be queryable via ContentChild in ViewContainerComponent
}
