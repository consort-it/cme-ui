import { DomainModelViewModule } from './domain-model-view.module';

describe('DomainModelViewModule', () => {
  it('should work', () => {
    expect(new DomainModelViewModule()).toBeDefined();
  });
});
